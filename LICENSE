# ERA Coin Blockchain Setup using Cosmos SDK
# Step-by-step guide with code snippets to create a native blockchain named ERA with its own coin

# 1. Prerequisites
sudo apt update && sudo apt upgrade -y
sudo apt install build-essential git curl jq make gcc -y

# Install Go (at least version 1.20)
wget https://go.dev/dl/go1.20.5.linux-amd64.tar.gz
sudo tar -C /usr/local -xzf go1.20.5.linux-amd64.tar.gz
echo 'export PATH=$PATH:/usr/local/go/bin' >> ~/.bashrc
source ~/.bashrc

go version

# 2. Clone Cosmos SDK Starter Template
cd $HOME
git clone https://github.com/cosmos/sdk-tutorials.git
cd sdk-tutorials/starport/planet

# Optional: Use Starport for easy setup
# Install Starport (Node/Validator scaffolding tool)
curl https://get.starport.network/starport! | bash
starport version

# 3. Create Blockchain (ERA) with Starport
starport scaffold chain github.com/username/era --address-prefix era
cd era

# 4. Modify Configuration
# a. Change coin name to ERA
sed -i 's/stake/era/g' config.yml

# b. Set token supply (in genesis)
# You’ll manually edit: config/genesis.json after init (step 6)

# 5. Scaffold module if needed
starport scaffold module bank

# 6. Build & Initialize the Blockchain
starport chain serve

# Alternatively (manually):
# make install
# era init my-node --chain-id era-chain

# 7. Edit genesis.json to set initial supply (20 million ERA)
# Add inside app_state > bank > balances:

#    "balances": [
#      {
#        "address": "<your_address>",
#        "coins": [
#          {
#            "denom": "era",
#            "amount": "20000000"
#          }
#        ]
#      }
#    ],

# 8. Start the Blockchain Node
era start

# 9. Wallet & Transactions (CLI)
# Create wallet
era keys add mywallet
# Get address
era keys show mywallet -a
# Send coins
era tx bank send <sender> <receiver> 100era --chain-id era-chain

# 10. Setup Explorer (optional)
# You can integrate with Big Dipper, Ping.pub, or build custom Vue/React explorer

# ---
# Upload Content Guide
# All content (config files, code, genesis.json) will be in the era/ folder
# You can zip this folder and upload to:
# Google Drive / GitHub / IPFS / Dropbox / your own hosting

# Recommend: Create GitHub repo
# Steps:
#   1. cd era
#   2. git init
#   3. git add . && git commit -m "Initial ERA blockchain setup"
#   4. git remote add origin https://github.com/yourname/era-chain.git
#   5. git push -u origin main
